# Nutri-Chef-AI
NutriChef AI uses a fine-tuned transformer to generate structured recipes from prompts. It extracts ingredients to estimate nutritional values via API integration. The system showcases a full ML pipeline from fine-tuning to a functional UI. 
